# SmartGuard 全民反诈智能助手 - 使用指南及详细方案

## 目录

1. [项目概述](#1-项目概述)
2. [快速开始](#2-快速开始)
3. [用户使用指南](#3-用户使用指南)
   - 3.8 [邮件监控功能](#38-邮件监控功能)
4. [管理员使用指南](#4-管理员使用指南)
5. [API接口文档](#5-api接口文档)
6. [系统架构详细设计](#6-系统架构详细设计)
7. [部署指南](#7-部署指南)
8. [测试数据集说明](#8-测试数据集说明)
9. [评估报告](#9-评估报告)

---

## 1. 项目概述

### 1.1 项目背景

随着诈骗案件的高发，全民反诈已成为国家和社会的重要任务。本项目基于大语言模型（LLM）和多模态大模型（VLM），设计并开发了一个具备"感知-决策-干预-进化"能力的智能助手。

### 1.2 项目目标

- 提供多模态输入支持（文本、语音、图像、视频）
- 实现智能识别与决策引擎
- 提供实时干预与交互能力
- 构建自适应进化模块
- **支持邮件监控，自动检测邮箱中的可疑邮件**

### 1.3 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户层                               │
│   ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐      │
│   │  Web    │  │  APP    │  │小程序   │  │  API    │      │
│   └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘      │
└────────┼─────────────┼─────────────┼─────────────┼──────────┘
         │             │             │             │
┌────────┼─────────────┼─────────────┼─────────────┼──────────┐
│        ▼             ▼             ▼             ▼          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                   多模态输入层                        │   │
│  │   文本处理  │  语音识别  │  图像识别  │  视频分析   │   │
│  │   邮件监控  │                                           │   │  ← 新增
│  └─────────────────────────────────────────────────────┘   │
│                           │                                │
│  ┌────────────────────────┼────────────────────────────────┐│
│  │                        ▼                                 ││
│  │  ┌──────────┬──────────┬──────────┬──────────┐          ││
│  │  │ 感知模块 │→│ 决策模块 │→│ 干预模块 │→│ 进化模块 │          ││
│  │  └──────────┴──────────┴──────────┴──────────┘          ││
│  │                      Agent核心编排                       ││
│  └──────────────────────────────────────────────────────────┘│
│                           │                                 │
│  ┌────────────────────────┼────────────────────────────────┐│
│  │                        ▼                                 ││
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐   ││
│  │  │ SQLite  │  │ VectorDB│  │  Redis  │  │  File   │   ││
│  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘   ││
│  │                      数据存储层                          ││
│  └──────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 快速开始

### 2.1 环境要求

| 组件 | 最低要求 | 推荐配置 |
|------|----------|----------|
| CPU | 4核 | 8核+ |
| 内存 | 8GB | 16GB+ |
| 硬盘 | 20GB | 50GB+ SSD |
| Python | 3.9+ | 3.10+ |
| Docker | 20.10+ | 最新版 |

### 2.2 安装步骤

#### 2.2.1 克隆项目

```bash
git clone <repository_url>
cd agentnew/agent
```

#### 2.2.2 创建虚拟环境

```bash
# 创建虚拟环境
python -m venv agent_new

# 激活虚拟环境
# Windows
.\agent_new\Scripts\activate
# Linux/Mac
source agent_new/bin/activate
```

#### 2.2.3 安装依赖

```bash
pip install -r requirements.txt
```

#### 2.2.4 配置环境变量

创建 `.env` 文件：

```env
# API配置
DASHSCOPE_API_KEY=your_api_key_here
QWEN_MODEL=qwen-turbo

# 数据库配置
DATABASE_PATH=./data/anti_fraud.db

# 向量模型路径
KNOWLEDGE_BASE_PATH=./knowledge_base
LOCAL_MODEL_PATH=./models/text2vec
```

#### 2.2.5 启动服务

```bash
# 启动后端API服务
python -m uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --reload

# 或使用脚本
python src/api/main.py
```

### 2.3 Docker部署

```bash
# 构建镜像
docker build -t smartguard:latest .

# 运行容器
docker run -d -p 8000:8000 \
  -e DASHSCOPE_API_KEY=your_key \
  smartguard:latest
```

---

## 3. 用户使用指南

### 3.1 注册与登录

#### 3.1.1 普通用户注册

```
POST /api/v1/auth/register
{
  "username": "user123",
  "password": "password123",
  "email": "user@example.com"
}
```

#### 3.1.2 用户登录

```
POST /api/v1/auth/login
{
  "username": "user123",
  "password": "password123",
  "portal": "user"
}
```

### 3.2 风险分析功能

#### 3.2.1 文本风险分析

**入口**: 首页 → 风险分析 → 文本输入

```
POST /api/v1/chat
Content-Type: multipart/form-data

text: "【高风险】这里是可疑内容..."
user_id: "user_xxx"
mode: "risk"
```

**响应示例**:

```json
{
  "response": "检测到潜在诈骗风险：投资理财诈骗\n风险等级：4（紧急）",
  "risk_assessment": {
    "risk_level": 4,
    "risk_type": "investment_fraud",
    "analysis": "检测到投资理财相关关键词",
    "confidence": 0.92
  },
  "suggestions": [
    "不要轻信任何高回报投资",
    "核实对方身份和资质",
    "不要转账给陌生人"
  ],
  "guardian_notification": {
    "notified": true,
    "guardian_name": "张三",
    "notify_time": "2026-03-30 10:30:00"
  }
}
```

#### 3.2.2 多模态分析

**入口**: 首页 → 风险分析 → 多模态上传

```
POST /api/v1/analyze/multimodal
Content-Type: multipart/form-data

text: 可疑聊天记录
audio: <file>
image: <file>
user_id: "user_xxx"
```

### 3.3 智能对话功能

#### 3.3.1 反诈助手模式

```
POST /api/v1/chat
text: "我最近收到一条短信说中了大奖"
user_id: "user_xxx"
mode: "chat"
```

#### 3.3.2 知识学习模式

```
POST /api/v1/chat
text: "给我讲讲杀猪盘诈骗的套路"
user_id: "user_xxx"
mode: "learn"
```

### 3.4 监护人管理

#### 3.4.1 添加监护人

```
POST /api/v1/guardians
{
  "user_id": "user_xxx",
  "name": "张三",
  "phone": "13800138000",
  "relationship": "父亲"
}
```

#### 3.4.2 查看监护人

```
GET /api/v1/guardians?user_id=user_xxx
```

### 3.5 预警管理

#### 3.5.1 查看预警列表

```
GET /api/v1/users/{user_id}/alerts?unread_only=false
```

#### 3.5.2 确认预警

```
POST /api/v1/alerts/{alert_id}/acknowledge
{
  "user_id": "user_xxx"
}
```

### 3.6 举报功能

#### 3.6.1 提交举报

```
POST /api/v1/report/submit
{
  "user_id": "user_xxx",
  "scam_type": "investment_fraud",
  "content": "详细描述诈骗经过...",
  "evidence": "相关证据材料"
}
```

### 3.7 报告生成

#### 3.7.1 生成安全报告

```
POST /api/v1/reports/generate
{
  "user_id": "user_xxx",
  "report_type": "daily"
}
```

### 3.8 邮件监控功能

邮件监控功能可以自动检测邮箱中的可疑邮件，支持QQ邮箱、Gmail、163邮箱、Outlook等主流邮箱。

#### 3.8.1 添加邮件监控配置

```
POST /api/email-monitor/config
Authorization: Bearer <user_token>
{
  "email_address": "yourname@qq.com",
  "username": "yourname@qq.com",
  "password": "xxxxx"  // QQ邮箱使用授权码
}
```

**提示：QQ邮箱需要使用「授权码」而不是登录密码。**
获取方法：QQ邮箱 → 设置 → 账户 → POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务 → 开启IMAP服务 → 生成授权码

#### 3.8.2 获取监控配置列表

```
GET /api/email-monitor/configs
Authorization: Bearer <user_token>
```

#### 3.8.3 测试邮箱连接

```
GET /api/email-monitor/test-connection?email_address=xxx@qq.com&password=xxx
```

#### 3.8.4 删除邮件监控配置

```
DELETE /api/email-monitor/config/{config_id}
Authorization: Bearer <user_token>
```

#### 3.8.5 查看邮件预警

```
GET /api/email-monitor/alerts?limit=50
Authorization: Bearer <user_token>
```

响应示例：
```json
{
  "alerts": [
    {
      "id": "eml_xxx",
      "subject": "您的账户存在异常，请立即验证",
      "sender": "security@fake-bank.com",
      "scam_score": 0.85,
      "risk_level": 4,
      "risk_type": "phishing",
      "analysis": "这是一封钓鱼邮件...",
      "keywords": ["账户异常", "验证"]
    }
  ],
  "total": 10,
  "unread_count": 3,
  "llm_analysis_enabled": true
}
```

#### 3.8.6 标记预警已读

```
POST /api/email-monitor/alerts/{alert_id}/read
Authorization: Bearer <user_token>
```

#### 3.8.7 风险等级说明

| 等级 | 说明 |
|------|------|
| 0 | 安全 |
| 1 | 低风险 |
| 2 | 中等风险 |
| 3 | 高风险 |
| 4 | 极高风险 |
| 5 | 确认诈骗 |

---

## 4. 管理员使用指南

### 4.1 管理员登录

```
POST /api/v1/auth/login
{
  "username": "admin",
  "password": "admin123",
  "portal": "admin"
}
```

### 4.2 用户管理

#### 4.2.1 查看用户列表

```
GET /api/v1/admin/users?page=1&page_size=20
Authorization: Bearer <admin_token>
```

#### 4.2.2 创建用户

```
POST /api/v1/admin/users
Authorization: Bearer <admin_token>
{
  "username": "newuser",
  "password": "password123",
  "role": "user",
  "email": "newuser@example.com"
}
```

#### 4.2.3 更新用户

```
PUT /api/v1/admin/users/{user_id}
Authorization: Bearer <admin_token>
{
  "is_active": false,
  "role": "admin"
}
```

#### 4.2.4 删除用户

```
DELETE /api/v1/admin/users/{user_id}
Authorization: Bearer <admin_token>
```

### 4.3 举报审核

#### 4.3.1 查看待审核举报

```
GET /api/v1/admin/reports/pending?page=1&page_size=20
Authorization: Bearer <admin_token>
```

#### 4.3.2 审核举报

```
POST /api/v1/admin/reports/review
Authorization: Bearer <admin_token>
{
  "report_ids": ["report_xxx", "report_yyy"],
  "action": "verify",
  "reason": "确认诈骗，已学习入库"
}
```

### 4.4 操作日志

#### 4.4.1 查看操作日志

```
GET /api/v1/admin/logs?page=1&page_size=20
Authorization: Bearer <admin_token>
```

#### 4.4.2 筛选操作日志

```
GET /api/v1/admin/logs?action=create_user&start_time=1709000000&end_time=1709100000
Authorization: Bearer <admin_token>
```

#### 4.4.3 查看操作统计

```
GET /api/v1/admin/logs/statistics
Authorization: Bearer <admin_token>
```

### 4.5 知识库管理

#### 4.5.1 查看已学习关键词

```
GET /api/v1/admin/evolution/keywords
Authorization: Bearer <admin_token>
```

#### 4.5.2 查看已学习模式

```
GET /api/v1/admin/evolution/patterns
Authorization: Bearer <admin_token>
```

### 4.6 统计信息

#### 4.6.1 用户统计

```
GET /api/v1/admin/users/stats/overview
Authorization: Bearer <admin_token>
```

#### 4.6.2 举报统计

```
GET /api/v1/admin/reports/statistics
Authorization: Bearer <admin_token>
```

---

## 5. API接口文档

### 5.1 认证接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/v1/auth/register | 用户注册 |
| POST | /api/v1/auth/login | 用户登录 |
| POST | /api/v1/auth/logout | 用户登出 |
| GET | /api/v1/auth/me | 获取当前用户信息 |
| POST | /api/v1/auth/refresh | 刷新Token |

### 5.2 分析接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/v1/analyze | 综合分析接口 |
| POST | /api/v1/analyze/text | 文本分析 |
| POST | /api/v1/analyze/multimodal | 多模态分析 |
| POST | /api/v1/chat | 通用对话接口 |

### 5.3 用户接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/v1/users/{user_id}/profile | 获取用户画像 |
| PUT | /api/v1/users/{user_id}/profile | 更新用户画像 |
| GET | /api/v1/users/{user_id}/guardians | 获取监护人列表 |
| POST | /api/v1/users/{user_id}/guardians | 添加监护人 |
| GET | /api/v1/users/{user_id}/alerts | 获取预警列表 |

### 5.4 管理员接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/v1/admin/users | 获取用户列表 |
| POST | /api/v1/admin/users | 创建用户 |
| PUT | /api/v1/admin/users/{id} | 更新用户 |
| DELETE | /api/v1/admin/users/{id} | 删除用户 |
| GET | /api/v1/admin/logs | 获取操作日志 |
| GET | /api/v1/admin/logs/statistics | 获取日志统计 |
| GET | /api/v1/admin/reports/pending | 获取待审核举报 |
| POST | /api/v1/admin/reports/review | 审核举报 |

### 5.5 知识库接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/v1/knowledge/scam-types | 获取诈骗类型列表 |
| GET | /api/v1/knowledge/cases | 获取相似案例 |
| GET | /api/v1/knowledge/statistics | 获取知识库统计 |

---

## 6. 系统架构详细设计

### 6.1 数据库设计

#### 6.1.1 用户认证表 (users_auth)

```sql
CREATE TABLE users_auth (
    id TEXT PRIMARY KEY,
    username TEXT UNIQUE NOT NULL,
    email TEXT UNIQUE,
    phone TEXT UNIQUE,
    password_hash TEXT NOT NULL,
    role TEXT DEFAULT 'user',
    is_active INTEGER DEFAULT 1,
    created_at REAL,
    updated_at REAL,
    last_login REAL
);
```

#### 6.1.2 用户画像表 (user_profiles)

```sql
CREATE TABLE user_profiles (
    user_id TEXT PRIMARY KEY,
    nickname TEXT DEFAULT '',
    avatar_url TEXT DEFAULT '',
    bio TEXT DEFAULT '',
    age_group TEXT DEFAULT '',
    gender TEXT DEFAULT '',
    location TEXT DEFAULT '',
    occupation TEXT DEFAULT '',
    education TEXT DEFAULT '',
    risk_awareness INTEGER DEFAULT 50,
    experience_level TEXT DEFAULT '新手',
    interested_scam_types TEXT DEFAULT '[]',
    total_consultations INTEGER DEFAULT 0,
    reported_scams INTEGER DEFAULT 0,
    family_protected INTEGER DEFAULT 0,
    learned_topics TEXT DEFAULT '[]',
    quiz_scores TEXT DEFAULT '{}',
    consultation_count INTEGER DEFAULT 0,
    risk_count INTEGER DEFAULT 0,
    updated_at REAL
);
```

#### 6.1.3 预警记录表 (alerts)

```sql
CREATE TABLE alerts (
    id TEXT PRIMARY KEY,
    user_id TEXT,
    level INTEGER,
    risk_type TEXT,
    message TEXT,
    content TEXT,
    response TEXT,
    acknowledged INTEGER DEFAULT 0,
    acknowledged_at REAL,
    guardian_notified INTEGER DEFAULT 0,
    guardian_notifications TEXT DEFAULT '[]',
    created_at REAL
);
```

#### 6.1.4 管理员操作日志表 (admin_operation_logs)

```sql
CREATE TABLE admin_operation_logs (
    id TEXT PRIMARY KEY,
    admin_id TEXT,
    admin_username TEXT,
    action TEXT,
    target_type TEXT,
    target_id TEXT,
    details TEXT,
    ip_address TEXT,
    user_agent TEXT,
    created_at REAL
);
```

### 6.2 核心模块设计

#### 6.2.1 Agent核心模块

```python
class AntiFraudAgent:
    """多模态反诈智能体"""
    
    def __init__(self, user_id, llm_client, knowledge_base_path, ...):
        self.user_id = user_id
        self.llm_client = llm_client
        self.memory = UserMemory(user_id)
        self.knowledge_retriever = KnowledgeRetriever(knowledge_base_path)
        self.alert_manager = AlertManager()
        self.guardian_notifier = GuardianNotifier()
        
    async def process(self, input_data: AgentInput) -> AgentOutput:
        # 1. 感知：多模态输入处理
        perceived = await self._perceive(input_data)
        
        # 2. 决策：风险评估
        decision = await self._decide(perceived)
        
        # 3. 干预：执行预警
        await self._intervene(decision)
        
        # 4. 进化：学习新知识
        await self._evolve(decision)
        
        return decision
```

#### 6.2.2 意图识别模块

```python
class IntentRecognizer:
    """意图识别器"""
    
    async def recognize(self, text: str, context: Dict) -> IntentResult:
        # 使用Prompt Engineering进行意图识别
        prompt = self._build_intent_prompt(text, context)
        result = await self.llm_client.chat([{"role": "user", "content": prompt}])
        return self._parse_intent_result(result)
```

#### 6.2.3 风险评估模块

```python
class RiskEvaluator:
    """风险评估器"""
    
    async def evaluate(self, input_data, user_profile) -> RiskAssessment:
        # 1. 关键词检测
        keyword_level = self._keyword_check(input_data.text)
        
        # 2. 案例匹配
        similar_cases = await self._match_cases(input_data.text)
        
        # 3. LLM分析
        llm_analysis = await self._llm_analyze(input_data, user_profile)
        
        # 4. 综合评分
        final_score = self._calculate_score(keyword_level, similar_cases, llm_analysis)
        
        return RiskAssessment(
            risk_level=final_score.level,
            risk_type=final_score.type,
            confidence=final_score.confidence,
            analysis=final_score.analysis
        )
```

### 6.3 向量数据库设计

```python
# 向量数据库集合
collections = {
    "scam_cases": {
        "description": "诈骗案例库",
        "dimensions": 768,
        "fields": ["case_id", "scam_type", "content", "risk_level", "metadata"]
    },
    "knowledge_base": {
        "description": "反诈知识库",
        "dimensions": 768,
        "fields": ["kb_id", "title", "content", "category", "tags"]
    },
    "learned_patterns": {
        "description": "学习到的诈骗模式",
        "dimensions": 768,
        "fields": ["pattern_id", "scam_type", "pattern", "confidence", "learned_at"]
    }
}
```

---

## 7. 部署指南

### 7.1 单机部署

#### 7.1.1 系统要求

- Ubuntu 20.04+ / Windows Server 2019+
- Python 3.9+
- 8GB+ RAM
- 20GB+ 存储空间

#### 7.1.2 安装步骤

```bash
# 1. 安装系统依赖
sudo apt update
sudo apt install -y python3.10 python3-pip nginx

# 2. 创建应用目录
sudo mkdir -p /opt/smartguard
sudo cp -r . /opt/smartguard/

# 3. 安装Python依赖
cd /opt/smartguard
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 4. 配置服务
sudo nano /etc/systemd/system/smartguard.service

# 5. 启动服务
sudo systemctl daemon-reload
sudo systemctl enable smartguard
sudo systemctl start smartguard
```

#### 7.1.3 Nginx配置

```nginx
server {
    listen 80;
    server_name your_domain.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /ws {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

### 7.2 Docker Compose部署

```yaml
version: '3.8'

services:
  smartguard-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DASHSCOPE_API_KEY=${DASHSCOPE_API_KEY}
      - DATABASE_PATH=/data/anti_fraud.db
    volumes:
      - ./data:/data
      - ./knowledge_base:/app/knowledge_base
    restart: unless-stopped

  smartguard-web:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./web/dist:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - smartguard-api
    restart: unless-stopped
```

### 7.3 Kubernetes部署

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartguard-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: smartguard-api
  template:
    metadata:
      labels:
        app: smartguard-api
    spec:
      containers:
      - name: api
        image: smartguard:latest
        ports:
        - containerPort: 8000
        env:
        - name: DASHSCOPE_API_KEY
          valueFrom:
            secretKeyRef:
              name: smartguard-secrets
              key: api-key
---
apiVersion: v1
kind: Service
metadata:
  name: smartguard-api
spec:
  selector:
    app: smartguard-api
  ports:
  - port: 80
    targetPort: 8000
```

---

## 8. 测试数据集说明

### 8.1 数据集概述

我们构建了一个包含20+案例的标准化测试集，用于评估智能体的性能。

### 8.2 数据集结构

```
data/test_dataset/
├── README.md
├── cases.json
├── black_samples/      # 诈骗样本（黑样本）
│   ├── text/          # 文本案例
│   ├── audio/         # 音频案例
│   └── image/         # 图片案例
└── white_samples/      # 正常样本（白样本）
    ├── text/
    ├── audio/
    └── image/
```

### 8.3 案例分类

#### 8.3.1 黑样本（诈骗案例）

| 编号 | 模态 | 诈骗类型 | 风险等级 | 描述 |
|------|------|----------|----------|------|
| T001 | 文本 | 冒充公检法 | 4 | 骗子冒充警察要求转账 |
| T002 | 文本 | 投资理财 | 4 | 高回报投资诱惑 |
| T003 | 文本 | 兼职刷单 | 3 | 刷单返利骗局 |
| T004 | 文本 | 虚假贷款 | 3 | 无抵押快速贷款 |
| T005 | 文本 | 杀猪盘 | 4 | 网恋诱导投资 |
| T006 | 文本 | AI语音诈骗 | 4 | 冒充熟人借钱 |
| T007 | 文本 | 虚假征信 | 3 | 影响征信威胁 |
| T008 | 文本 | 购物退款 | 2 | 退货退款陷阱 |
| T009 | 文本 | 游戏交易 | 2 | 游戏装备交易 |
| T010 | 文本 | 追星诈骗 | 2 | 明星粉丝群诈骗 |
| A001 | 音频 | AI语音合成 | 4 | 合成语音诈骗 |
| I001 | 图片 | 深度伪造 | 4 | AI换脸诈骗 |

#### 8.3.2 白样本（正常内容）

| 编号 | 模态 | 描述 |
|------|------|------|
| N001 | 文本 | 正常聊天对话 |
| N002 | 文本 | 朋友借钱 |
| N003 | 文本 | 购物咨询 |
| N004 | 文本 | 工作交流 |
| N005 | 文本 | 反诈咨询 |
| N006 | 文本 | 正常短信 |
| N007 | 文本 | 家人问候 |
| N008 | 文本 | 工作通知 |
| N009 | 文本 | 银行短信 |
| N010 | 文本 | 快递通知 |

### 8.4 评估脚本

```python
# 运行测试
python -m src.api.main --test-mode

# 生成报告
python scripts/evaluation_report.py --input results.json --output report.md
```

---

## 9. 评估报告

### 9.1 评估指标

#### 9.1.1 分类指标

| 指标 | 计算方式 | 要求 |
|------|----------|------|
| 准确率 (Accuracy) | (TP+TN)/(TP+TN+FP+FN) | >90% |
| 精确率 (Precision) | TP/(TP+FP) | >85% |
| 召回率 (Recall) | TP/(TP+FN) | >85% |
| F1-score | 2*Precision*Recall/(P+R) | >85% |
| 误报率 (FPR) | FP/(FP+TN) | <5% |

#### 9.1.2 性能指标

| 指标 | 要求 | 说明 |
|------|------|------|
| 响应时间 | <10s | 文本/图片分析 |
| 并发能力 | ≥100 QPS | 峰值负载 |
| 可用性 | >99.9% | 系统正常运行时间 |

### 9.2 评估结果模板

```markdown
# SmartGuard 评估报告

## 测试环境

- 测试日期：2026-03-30
- 测试版本：v1.0.0
- 测试样本数：20+
- 测试设备：[硬件配置]

## 分类性能

| 指标 | 结果 | 要求 | 状态 |
|------|------|------|------|
| 准确率 | 92.5% | >90% | ✅ 通过 |
| 精确率 | 90.2% | >85% | ✅ 通过 |
| 召回率 | 91.8% | >85% | ✅ 通过 |
| F1-score | 91.0% | >85% | ✅ 通过 |
| 误报率 | 3.2% | <5% | ✅ 通过 |

## 响应时间

| 模态 | 平均响应时间 | 最大响应时间 | 要求 |
|------|--------------|--------------|------|
| 文本 | 2.3s | 5.1s | <10s |
| 图片 | 4.5s | 8.2s | <10s |
| 音频 | 8.7s | 12.3s | <15s |

## 诈骗类型覆盖

| 诈骗类型 | 识别准确率 | 召回率 |
|----------|------------|--------|
| 冒充公检法 | 95.2% | 93.5% |
| 投资理财 | 92.1% | 91.8% |
| 兼职刷单 | 94.5% | 92.0% |
| 虚假贷款 | 91.3% | 90.5% |
| ... | ... | ... |

## 功能测试

| 功能模块 | 测试用例数 | 通过数 | 状态 |
|----------|------------|--------|------|
| 文本分析 | 15 | 14 | ✅ |
| 音频分析 | 3 | 3 | ✅ |
| 图像分析 | 2 | 2 | ✅ |
| 预警推送 | 5 | 5 | ✅ |
| 监护联动 | 3 | 3 | ✅ |

## 结论

[填写评估结论]
```

---

## 附录

### A. 术语表

| 术语 | 说明 |
|------|------|
| LLM | Large Language Model，大语言模型 |
| VLM | Vision Language Model，视觉语言模型 |
| ASR | Automatic Speech Recognition，自动语音识别 |
| OCR | Optical Character Recognition，光学字符识别 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| Prompt | 提示词，用于引导AI模型输出的文本 |

### B. 参考资料

1. 公安部反诈宣传资料
2. 国家互联网应急中心数据
3. 通义千问API文档
4. Whisper语音识别文档

### C. 联系方式

- **技术支持邮箱**: support@smartguard.com
- **项目主页**: https://github.com/your-repo/smartguard
